{
  "name": "pintail-browser-smoke",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun test",
    "smoke": "bun run run.ts",
    "auth": "bun run auth.ts",
    "gate": "bun test && bun run auth && bun run smoke"
  },
  "dependencies": {
    "mysql2": "^3.15.3",
    "playwright": "^1.58.2"
  }
}
